#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,i;
    cin>>n;
    int arr[n];
    int length=sizeof(arr)/sizeof(arr[0]);
    for(i=0;i<length;i++)
        cin>>arr[i];
    for(i=0;i<length;i++)
        cout<<arr[i];







}
