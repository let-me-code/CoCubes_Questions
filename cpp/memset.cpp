#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a[5];

    // all elements of A are zero
    memset(a, -1, sizeof(a));
    for (int i = 0; i < 5; i++)
        cout << a[i] << " ";
    cout << endl;



return 1;
}
